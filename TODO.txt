- Texture tool
- Model tool (glb/gltf)
- Shader tool (compiling shaders for various APIs, e.g., Metal/Vulkan)
- Package tool

- Merge into one tool `trialkit`

- Merge xmldata/gfx.cpp/hpp into asset/gfx.cpp/hpp, was gonna try to put
  xml data structures in there, but decided I'd leave it to be in asset/ if the
  file is a binary blob, and if the file is actually xml data, we can just put
  correct parsing and whatnot into xmldata/
